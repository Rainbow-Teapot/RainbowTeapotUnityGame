using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SideProp : MonoBehaviour
{
    public float speed { get; set; }
    private SidePropsSpawner spawner;

    // Start is called before the first frame update
    void Start()
    {
        
    }
}
